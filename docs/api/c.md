# C API reference

The C API is the authoritative native interface. It uses opaque handles and
length-delimited memory buffers so internal C5.0 structures do not become part
of the ABI.

```{doxygengroup} c50_c_api
:project: C50
:content-only:
```
